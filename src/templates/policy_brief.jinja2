<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ headline }}</title>
  <style>
    body { font-family: Georgia, serif; max-width: 800px; margin: 40px auto; padding: 0 20px; color: #333; }
    h1 { color: #1a365d; border-bottom: 3px solid #2b6cb0; padding-bottom: 10px; }
    h2 { color: #2b6cb0; margin-top: 30px; }
    .executive-summary { background: #ebf8ff; padding: 20px; border-left: 4px solid #2b6cb0; margin: 20px 0; }
    .recommendation { background: #f0fff4; padding: 15px; border-left: 4px solid #38a169; margin: 10px 0; }
    .data-callout { background: #fff5f5; padding: 15px; border-left: 4px solid #e53e3e; margin: 15px 0; font-size: 1.1em; }
    .footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid #ccc; font-size: 0.85em; color: #666; }
    table { border-collapse: collapse; width: 100%; margin: 15px 0; }
    th, td { border: 1px solid #ddd; padding: 8px 12px; text-align: left; }
    th { background: #2b6cb0; color: white; }
  </style>
</head>
<body>
  <h1>{{ headline }}</h1>
  <p><strong>Jurisdiction:</strong> {{ jurisdiction }} | <strong>Audience:</strong> {{ audience }} | <strong>Date:</strong> {{ date }}</p>

  {% if executive_summary %}
  <div class="executive-summary">
    <h2>Executive Summary</h2>
    <p>{{ executive_summary }}</p>
  </div>
  {% endif %}

  <div class="body-content">
    {{ body | safe }}
  </div>

  {% if call_to_action %}
  <div class="recommendation">
    <h2>Recommended Actions</h2>
    <p>{{ call_to_action }}</p>
  </div>
  {% endif %}

  {% if friction_scores %}
  <h2>Supporting Data</h2>
  <table>
    <thead>
      <tr><th>Topic</th><th>Friction Score</th><th>Estimated Cost</th></tr>
    </thead>
    <tbody>
      {% for item in friction_scores %}
      <tr>
        <td>{{ item.topic }}</td>
        <td>{{ item.friction_score }}</td>
        <td>${{ item.estimated_cost | default("N/A") }}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  {% endif %}

  <div class="footer">
    <p>Generated by HousingSpeak | {{ generated_by }} | {{ date }}</p>
  </div>
</body>
</html>
